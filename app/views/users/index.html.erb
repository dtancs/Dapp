<div class="container">
  <div class="row">
    <h4>Users</h4>

    <%= paginate @users %>

    <ul class="users">
      <% @users.each do |user| %>
        <%= render user %>
      <% end %>
    </ul>

    <%= paginate @users %>
    <br>

    <%= link_to 'New User', new_user_path %>

  </div>
</div>
