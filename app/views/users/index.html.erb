<div class="container">
  <div class="row">
    <h4>Users</h4>

    <%= paginate @users %>

    <ul class="users">
      <% @users.each do |user| %>
        <%= render user %>
      <% end %>
    </ul>

    <%= paginate @users %>
  </div>
</div>
